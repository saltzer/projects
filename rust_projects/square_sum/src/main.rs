fn main() {

    let square_sum(vec![1, 2, 3]);

    fn square_sum(vec: Vec<i32>) -> i32 {

        len sum = 0;
    
        //assert_eq!([1, 2], &array[1..]);
    
        // This loop prints: 0 1 2
        for x in array {
            sum += x*x;
            println!("{}", sum); 
        }

    }
}
//x.pow(n)